{
  "bootloader": "Systemd-boot",
  "custom_commands": [
    "for user in $(ls /home); do echo 'git clone -b dev https://github.com/seprog/arch-setup ~/arch-setup && cd ~/arch-setup && ./install.sh && rm ~/.bash_profile; cd ~' >> /home/$user/.bash_profile; done"
  ],
  "kernels": [ "linux-zen" ],
  "mirror_config": {
    "optional_repositories": [ "multilib" ]
  },
  "network_config": {
    "type": "iso"
  },
  "ntp": true,
  "packages": [
    "base-devel",
    "git"
  ],
  "parallel_downloads": 15,
  "swap": true
}
